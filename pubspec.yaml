name: calorie_tracker
description: A Calorie Tracker app with dark/light mode and EN/BN language support.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  fl_chart: ^0.70.0
  google_fonts: ^6.2.1
  percent_indicator: ^4.2.3
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  flutter_svg: ^2.0.10+1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
